---
phase: 04-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sync.py
  - tests/test_api.py
  - tests/test_parse.py
autonomous: true
requirements:
  - SC-01
  - SC-02
  - SC-03

must_haves:
  truths:
    - "dry-run prints pre-flight summary with note count, attachment count, metadata info, and lost fields before per-note lines"
    - "empty-body notes are imported with *(empty note)* placeholder content plus metadata footer"
    - "collection names with / or & are sanitized before API calls; state key uses original unsanitized name"
    - "sanitized collection names produce a Warning log line"
  artifacts:
    - path: "sync.py"
      provides: "sanitize_collection_name function, updated run_dry_run, updated run_import"
      contains: "sanitize_collection_name"
    - path: "tests/test_api.py"
      provides: "Tests for empty note import, sanitized collection names, dry-run pre-flight"
      contains: "TestSanitizeCollectionName"
  key_links:
    - from: "sync.py:run_dry_run"
      to: "sync.py:discover_notes"
      via: "pre-flight summary computed from all notes before per-note loop"
      pattern: "Pre-flight summary"
    - from: "sync.py:run_import"
      to: "sync.py:build_metadata_footer"
      via: "empty notes get placeholder body + footer"
      pattern: "empty note"
    - from: "sync.py:ensure_collection"
      to: "sync.py:sanitize_collection_name"
      via: "title sanitized before API request"
      pattern: "sanitize_collection_name"
---

<objective>
Harden sync.py with three code changes: (1) enhanced dry-run pre-flight summary, (2) empty-body notes imported with placeholder instead of skipped, (3) special character sanitization in collection names. Plus tests for all three.

Purpose: Make the script safe to hand to someone who did not write it — the dry-run output gives a trustworthy pre-flight report, edge cases don't crash the import.
Output: Updated sync.py with all hardening changes, updated test files with coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hardening/04-CONTEXT.md

@sync.py
@tests/test_api.py
@tests/test_parse.py
@tests/conftest.py

<interfaces>
<!-- Key functions the executor will modify or call -->

From sync.py:
```python
def run_dry_run(export_dir: Path) -> None:    # Lines 520-561 — add pre-flight summary block
def run_import(export_dir: Path, workspace_id: str, client: httpx.Client) -> None:  # Lines 346-456 — change empty note handling at lines 374-383
def ensure_collection(client, workspace_id, account, folder, state, state_path) -> str:  # Lines 216-249 — sanitize title before API call
def build_metadata_footer(created: datetime | None, modified: datetime | None) -> str:  # Reuse as-is for placeholder notes
def discover_notes(export_dir: Path) -> tuple[list[NoteFile], int]:  # Read-only, used in pre-flight
def parse_note(note_file: NoteFile) -> ParsedNote:  # Read-only, used in pre-flight scanning
def clean_body(body: str, title: str) -> str:  # Read-only, used in pre-flight scanning
```

From tests/conftest.py:
```python
@pytest.fixture
def sample_export(tmp_path):  # Creates iCloud/Notes/ with Note.md, Empty.md, My-Note.md + versioned snapshots
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sanitize_collection_name and update ensure_collection, run_import for empty notes, run_dry_run for pre-flight summary</name>
  <files>sync.py</files>
  <read_first>
    - sync.py (entire file — 620 lines)
    - .planning/phases/04-hardening/04-CONTEXT.md (decisions D-01 through D-10)
  </read_first>
  <behavior>
    - sanitize_collection_name("A/B") returns "A - B"
    - sanitize_collection_name("Rock & Roll") returns "Rock and Roll"
    - sanitize_collection_name("Normal Name") returns "Normal Name"
    - sanitize_collection_name("A/B & C") returns "A - B and C"
    - sanitize_collection_name strips control characters (e.g. \x00, \x1f)
    - run_import with an empty-body note creates an item with content "*(empty note)*" + metadata footer, status "imported"
    - run_import with an empty-body note does NOT skip (no "skipped_empty" status)
    - run_dry_run prints "Pre-flight summary:" block before any per-note lines
    - run_dry_run shows empty notes as "import (empty → placeholder): <title>" not "skip (empty)"
    - ensure_collection passes sanitized title to API but uses original string as state key
  </behavior>
  <action>
    1. Add `sanitize_collection_name(name: str) -> str` function (per D-08):
       - Replace `/` with ` - ` (space-dash-space)
       - Replace `&` with `and`
       - Strip control characters (unicodedata.category startswith "C")
       - Return stripped result

    2. Update `ensure_collection` (per D-09, D-10):
       - Call `sanitize_collection_name(account)` and `sanitize_collection_name(folder)` for the `"title"` field in both API calls (lines 228 and 242)
       - Keep `acct_key = account` and `folder_key = f"{account}/{folder}"` using ORIGINAL unsanitized strings (for idempotency)
       - If sanitized != original, log: `typer.echo(f"Warning: collection name sanitized: '{original}' → '{sanitized}'")`
       - Only log once per unique original name (use a module-level set `_sanitized_warnings: set[str] = set()`)

    3. Update `run_import` empty note block (lines 374-383, per D-04, D-05, D-06):
       - Replace the `if not body.strip():` block that currently sets `"skipped_empty"` and continues
       - Instead: set `body = "*(empty note)*"` when `not body.strip()`
       - Let execution continue to `footer = build_metadata_footer(...)` and the API call
       - The state entry will be `"status": "imported"` like any other note
       - Do NOT change the idempotency check at line 366 — `"skipped_empty"` entries from prior runs will be retried (correct per D-07)

    4. Update `run_dry_run` (per D-01, D-02, D-03, D-06):
       - BEFORE the per-note `for` loop, add a pre-scan loop that counts:
         - `total_notes` (all canonical)
         - `already_imported` (status == "imported" in state)
         - `notes_with_attachments` (have attachment_dir)
         - `empty_notes` (body.strip() is empty after clean_body)
         - `all_frontmatter_keys` (set of all keys across all notes)
       - After pre-scan, print:
         ```
         Pre-flight summary:
           Notes to import:    {total_notes - already_imported}
           Notes to skip:      {already_imported} (already imported)
           Attachments:        {notes_with_attachments} notes have attachments
           Metadata footer:    created, modified will be appended as HTML comment
           Lost fields:        {comma-separated keys not in {title, created, modified}} or "none"
         ```
       - Change the empty note line in the per-note loop from:
         `typer.echo(f"  skip (empty): {note_file.title}")` + `would_skip += 1`
         to:
         `typer.echo(f"  import (empty → placeholder): {note_file.title}")` + `would_import += 1`
  </action>
  <verify>
    <automated>cd /Users/thid/dev/nuclino-sync && python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sanitize_collection_name" sync.py` returns at least 3 (definition + 2 call sites)
    - `grep "empty note" sync.py` matches `*(empty note)*` placeholder string
    - `grep "Pre-flight summary" sync.py` matches at least 1 line
    - `grep "import (empty" sync.py` matches the new dry-run line format
    - `grep "skipped_empty" sync.py` returns 0 matches in run_import (removed), may still exist in run_parse_only
  </acceptance_criteria>
  <done>
    - sanitize_collection_name function exists and is called by ensure_collection
    - Empty notes flow through to API creation with placeholder body instead of being skipped
    - Dry-run prints pre-flight summary block before per-note lines
    - Dry-run shows empty notes as "import (empty -> placeholder)" not "skip (empty)"
    - All existing tests still pass (some may need updating for changed behavior)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests for sanitization, empty note import, and dry-run pre-flight summary</name>
  <files>tests/test_api.py, tests/test_parse.py</files>
  <read_first>
    - tests/test_api.py (existing test patterns)
    - tests/test_parse.py (existing test patterns)
    - tests/conftest.py (fixtures — sample_export has Empty.md)
    - sync.py (updated version from Task 1)
  </read_first>
  <behavior>
    - test_sanitize_slash: sanitize_collection_name("A/B") == "A - B"
    - test_sanitize_ampersand: sanitize_collection_name("R & D") == "R and D"
    - test_sanitize_combined: sanitize_collection_name("A/B & C") == "A - B and C"
    - test_sanitize_noop: sanitize_collection_name("Normal") == "Normal"
    - test_sanitize_control_chars: sanitize_collection_name("abc\x00def") == "abcdef"
    - test_ensure_collection_sanitizes_title: API call uses sanitized title, state key uses original
    - test_ensure_collection_logs_warning: typer.echo called with "Warning: collection name sanitized"
    - test_empty_note_imported_with_placeholder: run_import creates item with "*(empty note)*" content
    - test_empty_note_state_imported: state entry has status "imported" not "skipped_empty"
    - test_dry_run_preflight_summary: output contains "Pre-flight summary:" and note/attachment counts
    - test_dry_run_empty_shows_placeholder_line: output contains "import (empty → placeholder)"
  </behavior>
  <action>
    1. In tests/test_parse.py, add `TestSanitizeCollectionName` class:
       - Import `sanitize_collection_name` from sync
       - Test slash replacement: assert sanitize_collection_name("A/B") == "A - B"
       - Test ampersand replacement: assert sanitize_collection_name("R & D") == "R and D"
       - Test combined: assert sanitize_collection_name("A/B & C") == "A - B and C"
       - Test no-op: assert sanitize_collection_name("Normal") == "Normal"
       - Test control char stripping: assert sanitize_collection_name("abc\x00def") == "abcdef"

    2. In tests/test_api.py, add to `TestEnsureCollection`:
       - `test_sanitizes_title_in_api_call`: Create state with no collections. Call ensure_collection with account="A/B", folder="Notes". Assert the API call's json["title"] is "A - B" (sanitized), but state key is "A/B" (original).
       - `test_sanitize_warning_logged`: Same setup but capture stdout (capsys). Assert "Warning: collection name sanitized" in output.

    3. In tests/test_api.py, update `TestRunImport`:
       - Update `test_summary_output` to expect "Imported 3 notes" instead of "Imported 2 notes" (Empty.md is now imported, not skipped). sample_export has 3 canonical notes: Note.md, Empty.md, My-Note.md.
       - Add `test_empty_note_placeholder`: Mock api_request to succeed. After run_import, check the state dict for the Empty.md entry: assert status == "imported", and verify the API call for Empty.md used content containing "*(empty note)*".

    4. In tests/test_api.py, update `TestDryRun`:
       - Update `test_dry_run_output` to check for "import (empty → placeholder): Empty" instead of "skip (empty)"
       - Add `test_dry_run_preflight_summary`: run_dry_run(sample_export), capture output. Assert "Pre-flight summary:" in output. Assert "Notes to import:" in output. Assert "Attachments:" in output. Assert "Metadata footer:" in output.
       - Update `test_dry_run_summary` if count assertions change (empty note now counts as import not skip)

    5. Run full test suite: `python -m pytest tests/ -x -v`
  </action>
  <verify>
    <automated>cd /Users/thid/dev/nuclino-sync && python -m pytest tests/ -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -x -v` shows 0 failures
    - `grep -c "TestSanitizeCollectionName" tests/test_parse.py` returns 1
    - `grep -c "empty.*placeholder" tests/test_api.py` returns at least 2
    - `grep -c "Pre-flight summary" tests/test_api.py` returns at least 1
    - Total test count is at least 5 more than before (5 sanitize + 2 collection + 2 empty note + 2 dry-run updates)
  </acceptance_criteria>
  <done>
    - All new hardening behaviors have test coverage
    - Existing tests updated to reflect changed behavior (empty notes imported, not skipped)
    - Full test suite passes green
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -x -v` — all tests pass
2. `grep "sanitize_collection_name" sync.py` — function exists and is called
3. `grep "Pre-flight summary" sync.py` — pre-flight block exists in dry-run
4. `grep "\\*(empty note)\\*" sync.py` — placeholder exists in run_import
5. `grep "import (empty" sync.py` — dry-run shows new format for empty notes
</verification>

<success_criteria>
- sanitize_collection_name handles /, &, and control characters
- ensure_collection uses sanitized title for API but original for state key
- Empty notes are imported with "*(empty note)*" placeholder + metadata footer
- Dry-run pre-flight summary prints before per-note lines with counts and metadata info
- All tests pass including new tests for the three changes
</success_criteria>

<output>
After completion, create `.planning/phases/04-hardening/04-01-SUMMARY.md`
</output>
