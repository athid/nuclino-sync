---
phase: 04-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - SC-04

must_haves:
  truths:
    - "README contains a minimal working example: install, set env vars, run, verify output"
    - "README lists Python 3.11+ as prerequisite"
    - "README shows both NUCLINO_API_KEY and NUCLINO_WORKSPACE_ID env vars"
    - "README shows --dry-run example"
    - "README explains what nuclino-state.json is"
  artifacts:
    - path: "README.md"
      provides: "User-facing documentation"
      min_lines: 30
      contains: "NUCLINO_API_KEY"
  key_links: []
---

<objective>
Create README.md with a minimal working example covering install, env vars, run command, dry-run, and state file explanation.

Purpose: Make the script safe to hand to someone who did not write it — the README is the user's entry point.
Output: README.md in project root.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-hardening/04-CONTEXT.md

@sync.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/04-hardening/04-CONTEXT.md (decisions D-11, D-12, D-13)
    - sync.py (lines 563-619 — CLI definition for exact flag names and defaults)
  </read_first>
  <action>
    Create `README.md` in project root (per D-13, overwrite if exists). Content per D-11 and D-12:

    1. **Title and one-line description:** `# nuclino-sync` — Import Apple Notes exports into Nuclino.

    2. **Prerequisites section:**
       - Python 3.11+
       - pip
       - A Nuclino account with an API key

    3. **Install section:**
       ```
       pip install -e .
       ```

    4. **Configuration section:**
       - `NUCLINO_API_KEY` — required, from Nuclino Settings > API. The key value itself (no prefix needed).
       - `NUCLINO_WORKSPACE_ID` — required, workspace name or ID. Can also use `--workspace` flag.
       - `NOTES_EXPORT_DIR` — optional, defaults to `~/Desktop/NotesExport`. Can also use `--export-dir` flag.

       Show export commands:
       ```bash
       export NUCLINO_API_KEY="your-api-key"
       export NUCLINO_WORKSPACE_ID="My Workspace"
       ```

    5. **Usage section:**
       - Dry-run first (recommended): `python sync.py --dry-run`
         Explain: prints a pre-flight summary and per-note import plan without making any API calls.
       - Run import: `python sync.py`
       - Parse-only (offline): `python sync.py --parse-only`

    6. **State file section:**
       - The script writes `nuclino-state.json` in the export directory.
       - Tracks which notes and collections have been created.
       - Safe to re-run: already-imported notes are skipped.
       - If interrupted, the state file is not corrupted (atomic writes).

    7. **Footer:** Link to Nuclino API docs: https://help.nuclino.com/d3a29686-api (per D-12, no troubleshooting section).

    No emojis. No troubleshooting section. Keep it minimal and direct.
  </action>
  <verify>
    <automated>test -f /Users/thid/dev/nuclino-sync/README.md && grep -c "NUCLINO_API_KEY" /Users/thid/dev/nuclino-sync/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` succeeds
    - `grep "NUCLINO_API_KEY" README.md` matches at least 2 lines (description + export example)
    - `grep "NUCLINO_WORKSPACE_ID" README.md` matches at least 2 lines
    - `grep "dry-run" README.md` matches at least 1 line
    - `grep "nuclino-state.json" README.md` matches at least 1 line
    - `grep "3.11" README.md` matches at least 1 line
    - `grep "pip install" README.md` matches at least 1 line
    - `wc -l < README.md` is at least 30 lines
  </acceptance_criteria>
  <done>
    - README.md exists in project root with all sections from D-11
    - No troubleshooting section (per D-12)
    - Contains link to Nuclino API docs
    - Readable by someone who did not write the script
  </done>
</task>

</tasks>

<verification>
1. `cat README.md` — all sections present
2. `grep -c "NUCLINO_API_KEY" README.md` >= 2
3. `grep "dry-run" README.md` — dry-run example present
4. `grep "nuclino-state.json" README.md` — state file explained
</verification>

<success_criteria>
- README.md exists with install, env vars, usage, dry-run example, and state file explanation
- Minimal and direct — no troubleshooting, no extras beyond D-11 scope
- Someone unfamiliar with the project can follow it to run an import
</success_criteria>

<output>
After completion, create `.planning/phases/04-hardening/04-02-SUMMARY.md`
</output>
