---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/conftest.py
  - tests/test_parse.py
  - tests/test_state.py
autonomous: true
requirements:
  - PARSE-01
  - PARSE-02
  - PARSE-03
  - PARSE-04
  - PARSE-05
  - STATE-01
  - STATE-02
  - STATE-03

must_haves:
  truths:
    - "pytest discovers and runs all tests without error"
    - "Canonical vs versioned file filtering is tested with edge cases"
    - "Date parsing is tested against actual Apple Notes date strings"
    - "Body cleaning is tested for title-match, no-match, and empty cases"
    - "State roundtrip (save then load) preserves all data"
    - "Atomic write produces .tmp then replaces target"
    - "Re-run skips items already in state"
    - "Empty notes are recorded as skipped_empty"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Shared pytest fixtures for sample notes and export directories"
      min_lines: 30
    - path: "tests/test_parse.py"
      provides: "Tests for is_canonical, discover_notes, parse_note, parse_apple_date, clean_body"
      min_lines: 80
    - path: "tests/test_state.py"
      provides: "Tests for load_state, save_state, run_parse_only integration"
      min_lines: 60
  key_links:
    - from: "tests/test_parse.py"
      to: "sync.py"
      via: "import"
      pattern: "from sync import"
    - from: "tests/test_state.py"
      to: "sync.py"
      via: "import"
      pattern: "from sync import"
---

<objective>
Create a comprehensive pytest test suite covering all Phase 1 parsing and state management functions, ensuring correctness of the canonical filter, date parser, body cleaner, and atomic state management.

Purpose: Validates all 8 Phase 1 requirements through automated tests that can be run with `pytest` in under 10 seconds. Provides regression safety for Phase 2+ development.
Output: Three test files (conftest.py, test_parse.py, test_state.py) with full coverage of Phase 1 logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts from sync.py that tests must import and exercise -->

```python
from pathlib import Path
from datetime import datetime
from dataclasses import dataclass

@dataclass
class NoteFile:
    account: str
    folder: str
    title: str
    md_path: Path
    attachment_dir: Path | None

@dataclass
class ParsedNote:
    title: str
    body: str
    created: datetime | None
    modified: datetime | None
    attachment_dir: Path | None

def is_canonical(path: Path) -> bool: ...
def discover_notes(export_dir: Path) -> tuple[list[NoteFile], int]: ...
def parse_apple_date(value: str) -> datetime: ...
def parse_note(note_file: NoteFile) -> ParsedNote: ...
def clean_body(body: str, title: str) -> str: ...
def load_state(state_path: Path) -> dict: ...
def save_state(state: dict, state_path: Path) -> None: ...
def run_parse_only(export_dir: Path) -> None: ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create conftest.py and test_parse.py</name>
  <files>tests/conftest.py, tests/test_parse.py</files>
  <read_first>
    sync.py
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
Create `tests/` directory and two files:

**tests/conftest.py** — shared fixtures:

1. `sample_note(tmp_path)` fixture:
   - Creates `tmp_path / "iCloud" / "Notes" / "Test Note.md"` with content:
     ```
     ---
     title: Test Note
     created: Thursday, 12 September 2024 at 07:24:45
     modified: Friday, 13 September 2024 at 10:00:00
     ---

     Test Note
     This is the body content.
     ```
   - Returns the Path to the .md file

2. `sample_export(tmp_path)` fixture:
   - Creates `tmp_path / "iCloud" / "Notes"` directory
   - Creates canonical note `Note.md` with frontmatter (title: Note, created/modified dates) and body starting with "Note\nBody here."
   - Creates versioned snapshot `Note-1.md` with same content
   - Creates another versioned `Note-12.md` with same content
   - Creates empty note `Empty.md` with frontmatter but empty body (just whitespace after frontmatter)
   - Creates canonical note with hyphen in title `My-Note.md` (title: My-Note, body starting with "My-Note\nContent.")
   - Creates a second account dir `tmp_path / "anders@thib.se" / "Notes"` (empty directory)
   - Creates attachment directory `tmp_path / "iCloud" / "Notes" / "Note"` (sibling dir for Note.md) with a dummy file `image.png` inside
   - Returns `tmp_path`

3. `empty_export(tmp_path)` fixture:
   - Creates `tmp_path / "iCloud" / "Notes"` with no .md files
   - Returns `tmp_path`

**tests/test_parse.py** — parsing tests:

```python
from pathlib import Path
from datetime import datetime
from sync import is_canonical, discover_notes, parse_apple_date, parse_note, clean_body, NoteFile
```

Tests to implement:

1. `test_is_canonical_regular_note`: `is_canonical(Path("Note.md"))` returns `True`
2. `test_is_canonical_versioned_single_digit`: `is_canonical(Path("Note-1.md"))` returns `False`
3. `test_is_canonical_versioned_multi_digit`: `is_canonical(Path("Note-12.md"))` returns `False`
4. `test_is_canonical_hyphen_in_title`: `is_canonical(Path("My-Note.md"))` returns `True` (hyphen followed by non-digit)
5. `test_is_canonical_number_in_title`: `is_canonical(Path("80x90 rvg.md"))` returns `True`
6. `test_is_canonical_trailing_digit_no_hyphen`: `is_canonical(Path("Note2.md"))` returns `True` (no hyphen before digit)

7. `test_discover_notes_count(sample_export)`: discovers exactly 3 canonical notes (Note.md, Empty.md, My-Note.md) and counts 2 versioned
8. `test_discover_notes_accounts(sample_export)`: discovered notes include account "iCloud"; anders@thib.se directory exists but has no notes
9. `test_discover_notes_attachment_dir(sample_export)`: the NoteFile for Note.md has `attachment_dir` set to the `iCloud/Notes/Note` directory; Empty.md and My-Note.md have `attachment_dir=None`
10. `test_discover_notes_empty_export(empty_export)`: returns `([], 0)`

11. `test_parse_apple_date_2024`: `parse_apple_date("Thursday, 12 September 2024 at 07:24:45")` returns `datetime(2024, 9, 12, 7, 24, 45)`
12. `test_parse_apple_date_2013`: `parse_apple_date("Monday, 23 July 2012 at 00:00:00")` returns `datetime(2012, 7, 23, 0, 0, 0)` (verify it handles older dates)
13. `test_parse_apple_date_strips_whitespace`: `parse_apple_date("  Thursday, 12 September 2024 at 07:24:45  ")` succeeds without error

14. `test_parse_note_extracts_title(sample_note)`: create a NoteFile pointing to `sample_note`, call `parse_note()`, assert `result.title == "Test Note"`
15. `test_parse_note_extracts_dates(sample_note)`: assert `result.created == datetime(2024, 9, 12, 7, 24, 45)` and `result.modified == datetime(2024, 9, 13, 10, 0, 0)`
16. `test_parse_note_extracts_body(sample_note)`: assert `result.body` starts with `"Test Note"` (the duplicate title line — cleaning happens separately)

17. `test_clean_body_strips_matching_title`: `clean_body("Test Note\nBody content.", "Test Note")` returns `"Body content."`
18. `test_clean_body_preserves_non_matching`: `clean_body("Different first line\nBody.", "Test Note")` returns `"Different first line\nBody."`
19. `test_clean_body_empty_string`: `clean_body("", "Test Note")` returns `""`
20. `test_clean_body_strips_leading_blank_lines`: `clean_body("Test Note\n\n\nBody content.", "Test Note")` returns `"Body content."` (leading blank lines after title also stripped)
21. `test_clean_body_whitespace_only`: `clean_body("   \n  ", "Test Note")` returns `"   \n  "` (whitespace body with non-matching first line is unchanged)
  </action>
  <verify>
    <automated>cd /Users/thid/dev/nuclino-sync && python -m pytest tests/test_parse.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` contains `def sample_note(tmp_path):`
    - `tests/conftest.py` contains `def sample_export(tmp_path):`
    - `tests/conftest.py` contains `"Note-1.md"` (versioned fixture)
    - `tests/conftest.py` contains `"Note-12.md"` (multi-digit versioned fixture)
    - `tests/conftest.py` contains `"My-Note.md"` (hyphen-in-title fixture)
    - `tests/conftest.py` contains `anders@thib.se` (second account fixture)
    - `tests/test_parse.py` contains `from sync import is_canonical, discover_notes, parse_apple_date, parse_note, clean_body`
    - `tests/test_parse.py` contains `test_is_canonical_hyphen_in_title`
    - `tests/test_parse.py` contains `test_discover_notes_attachment_dir`
    - `tests/test_parse.py` contains `test_parse_apple_date_2024`
    - `tests/test_parse.py` contains `test_clean_body_strips_matching_title`
    - `python -m pytest tests/test_parse.py` exits with code 0
  </acceptance_criteria>
  <done>All 21 parsing tests pass. conftest.py provides reusable fixtures for sample notes, sample export directory, and empty export directory.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_state.py with state management and integration tests</name>
  <files>tests/test_state.py</files>
  <read_first>
    sync.py
    tests/conftest.py
    tests/test_parse.py
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
Create **tests/test_state.py**:

```python
import json
import os
from pathlib import Path
from sync import load_state, save_state, run_parse_only
```

Tests to implement:

**State load/save (STATE-01, STATE-03):**

1. `test_load_state_missing_file(tmp_path)`: `load_state(tmp_path / "state.json")` returns `{"version": 1, "items": {}}` when file doesn't exist
2. `test_load_state_valid_file(tmp_path)`: write a valid JSON state to `tmp_path / "state.json"`, call `load_state()`, assert it returns the written data
3. `test_load_state_corrupt_json(tmp_path)`: write `"not valid json"` to the state file, call `load_state()`, assert it returns fresh state `{"version": 1, "items": {}}` (doesn't crash)
4. `test_save_state_creates_file(tmp_path)`: call `save_state({"version": 1, "items": {}}, tmp_path / "state.json")`, assert file exists and is valid JSON
5. `test_save_state_roundtrip(tmp_path)`: save a state dict with items, load it back, assert equality
6. `test_save_state_atomic_uses_replace(tmp_path, monkeypatch)`: monkeypatch `os.replace` to record calls, call `save_state()`, assert `os.replace` was called with args ending in `.json.tmp` and `.json`
7. `test_save_state_no_tmp_leftover(tmp_path)`: call `save_state()`, assert no `.json.tmp` file remains after successful write (os.replace removes it)

**State schema correctness (STATE-01, D-04):**

8. `test_state_has_version_field(tmp_path)`: after `save_state` with default state, loaded JSON has `"version": 1`
9. `test_state_items_keyed_on_relative_path(sample_export)`: run `run_parse_only(sample_export)`, load resulting `nuclino-state.json`, assert keys look like `"iCloud/Notes/Note.md"` (relative, not absolute)

**Idempotency (STATE-02):**

10. `test_rerun_skips_processed(sample_export, capsys)`: run `run_parse_only(sample_export)` twice, capture output of second run — assert it still prints the same summary counts but does NOT print "skipped empty note:" on the second run (empty notes already recorded in state)
11. `test_rerun_does_not_duplicate_items(sample_export)`: run `run_parse_only(sample_export)` twice, load state, assert number of items in state equals number of canonical notes (no duplicates)

**Empty note handling (D-03, D-04):**

12. `test_empty_note_skipped_with_warning(sample_export, capsys)`: run `run_parse_only(sample_export)`, capture stdout, assert `"skipped empty note: Empty"` appears in output
13. `test_empty_note_recorded_in_state(sample_export)`: run `run_parse_only(sample_export)`, load state, find the Empty.md entry, assert `"status": "skipped_empty"`
14. `test_parsed_note_has_status_parsed(sample_export)`: run `run_parse_only(sample_export)`, load state, find Note.md entry, assert `"status": "parsed"`

**Summary output (D-06):**

15. `test_parse_only_summary_format(sample_export, capsys)`: run `run_parse_only(sample_export)`, capture stdout, assert output contains "canonical notes across", "accounts", "folders", "skipped (empty)", "versioned snapshots ignored"
  </action>
  <verify>
    <automated>cd /Users/thid/dev/nuclino-sync && python -m pytest tests/test_state.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_state.py` contains `from sync import load_state, save_state, run_parse_only`
    - `tests/test_state.py` contains `test_load_state_missing_file`
    - `tests/test_state.py` contains `test_load_state_corrupt_json`
    - `tests/test_state.py` contains `test_save_state_roundtrip`
    - `tests/test_state.py` contains `test_save_state_atomic_uses_replace`
    - `tests/test_state.py` contains `test_rerun_skips_processed`
    - `tests/test_state.py` contains `test_empty_note_skipped_with_warning`
    - `tests/test_state.py` contains `test_empty_note_recorded_in_state`
    - `tests/test_state.py` contains `test_parse_only_summary_format`
    - `tests/test_state.py` contains `"skipped_empty"`
    - `python -m pytest tests/test_state.py` exits with code 0
    - `python -m pytest tests/ -v` exits with code 0 (all tests pass together)
  </acceptance_criteria>
  <done>All 15 state management tests pass. Full test suite (test_parse.py + test_state.py) passes with `pytest tests/`. State atomicity, idempotency, empty note handling, and summary output are all verified.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -v` — all tests pass (36 total: 21 parse + 15 state)
2. Test coverage spans all 8 requirements: PARSE-01 through PARSE-05, STATE-01 through STATE-03
3. Edge cases covered: versioned files with multi-digit suffixes, hyphens in titles, empty bodies, corrupt state files, idempotent re-runs
</verification>

<success_criteria>
- All tests pass with `python -m pytest tests/ -v`
- Canonical file filtering edge cases tested (PARSE-01)
- Title stripping tested for match and no-match (PARSE-02)
- Date parsing tested against real Apple date strings (PARSE-03)
- Frontmatter extraction tested (PARSE-04)
- Attachment directory discovery tested (PARSE-05)
- State save/load roundtrip tested (STATE-01)
- Idempotent re-run tested (STATE-02)
- Atomic write mechanism tested (STATE-03)
- Empty note handling tested per D-03, D-04
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
